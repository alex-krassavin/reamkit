---
# The <title> / og:title (keyword-led for search). The visible hero heading is
# its own H1 in Home.astro — this frontmatter title is hidden on the home page.
title: 'Convert DOCX, XLSX, PPTX & PDF in the browser'
description: Ream is a TypeScript library that converts Word, Excel, PowerPoint and PDF — including the legacy .doc / .xls / .ppt — to PDF, SVG, HTML, DOCX or XLSX, right in the browser. No LibreOffice, no headless Office.
tableOfContents: false
---

import Home from '../../components/Home.astro';

<Home />
